# -*- mode: snippet -*-
# name : Capybara Integration Test
# key: captc
# --
class `(let ((fn (capitalize (file-name-nondirectory
                 (file-name-sans-extension
          				 (or (buffer-file-name)
			        	     (buffer-name (current-buffer))))))))
                (replace-regexp-in-string "_" ""  fn))` < CapybaraIntegrationTest
  $0
end
