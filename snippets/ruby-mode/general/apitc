# -*- mode: snippet -*-
# name : API Integration Test
# key: apitc
# --
class `(let ((fn (capitalize (file-name-nondirectory
                 (file-name-sans-extension
          				 (or (buffer-file-name)
			        	     (buffer-name (current-buffer))))))))
                (replace-regexp-in-string "_" ""  fn))` < APIIntegrationTestCase
  $0
end
