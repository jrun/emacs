(setq sql-connection-alist
      '((portal_dev (sql-product 'postgres)
                    (sql-port 5432)
                    (sql-server "localhost")
                    (sql-user "jtb")
                    (sql-database "portal_dev"))
        ))
